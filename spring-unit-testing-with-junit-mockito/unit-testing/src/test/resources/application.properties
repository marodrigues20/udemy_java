# When we add application.properties into test/resouces folder it will be overwrite
# the java/resources application.properties file when run the tests.

server.port=8085

spring.h2.console.enabled=false
spring.jpa.show-sql=false
